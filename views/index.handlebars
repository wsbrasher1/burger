
<h1>Eat-Da-Burger</h1>


<div class="container">
    
    <div class="row justify-content-center">
        <div class="col-md-4">
            <div class="row text-center">
                <div class="col-md-12">
                    <h3 class="mb-3"><strong>Burger Menu</strong></h3>
                </div>
            </div>
            {{#each burgers}}
            {{#unless this.devoured}}
            <div class="row align-items-center mb-2">
                <div class="col-md-8 col-sm-9">{{this.id}}. {{this.burger_name}}</div>
                <div class="col-md-4 col-sm-3 burger-btn text-right"><button type="button" data-id="{{this.id}}" class="btn btn-success btn-sm devour">Devour</button></div>
            </div>
            {{/unless}}
            {{/each}}
        </div>

        <div class="col-md-1"></div>

        <div class="col-md-4" id="devoured">
            <div class="row text-center">
                <div class="col-md-12">
                    <h3 class="mb-3"><strong>Devoured</strong></h3>
                </div>
            </div>
            {{#each burgers}}
            {{#if this.devoured}}
            <div class="row align-items-center mb-2 devoured">
                <div class="col-md-12">{{this.id}}. {{this.burger_name}}</div>
            </div>
            {{/if}}
            {{/each}}
        </div>
    </div>

    <div class="row justify-content-center text-center">
        <div class="col-md-6" id="subtext"><strong>
            Can't find what you're looking for on the menu?<br>
            Just enter your preferred burger below & hit submit!
        </strong></div>
    </div>

    <div class="row mt-3 text-center">
        <div class="col-md-12">
            <form class="form-inline justify-content-center">
                <div class="form-group mx-sm-3 mb-2">
                    <label for="burger-name" class="sr-only">Enter another option</label>
                    <input type="text" class="form-control" id="burger-name" placeholder="Enter another option">
                </div>
                <button type="submit" id="add-burger" class="btn btn-danger mb-2">Submit</button>
            </form>
        </div>
    </div>

</div>

    